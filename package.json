{
  "name": "beatmap",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd frontend && npm install && npm run build",
    "start": "cd frontend && npm start -- -p 5000 -H 0.0.0.0"
  }
}
